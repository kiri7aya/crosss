import 'package:flutter/material.dart';

class FavoriteShopsPage extends StatelessWidget {
  const FavoriteShopsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Your favorite shops will appear here.'),
    );
  }
}