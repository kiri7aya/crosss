import 'package:flutter/material.dart';

class MyPicksPage extends StatelessWidget {
  const MyPicksPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Your favorite flowers will appear here.'),
    );
  }
}